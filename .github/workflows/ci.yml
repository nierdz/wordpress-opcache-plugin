name: CI

on: push

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: install check tools
      run: make install
    - name: run all checks
      run: make tests
